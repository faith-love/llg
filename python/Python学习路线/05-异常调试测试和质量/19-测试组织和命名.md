# 测试组织和命名

测试结构应该让人快速找到被测对象、测试场景和失败原因。好的测试命名比测试数量更重要。

## 推荐目录

```text
project/
  src/
    text_tools/
      cleaner.py
      counter.py
  tests/
    test_cleaner.py
    test_counter.py
    conftest.py
```

小项目也可以：

```text
project/
  text_tools/
  tests/
```

## 测试文件命名

推荐：

```text
test_cleaner.py
test_counter.py
test_order.py
```

和被测模块对应，便于查找。

## 测试函数命名

推荐表达场景和期望：

```python
def test_clean_text_strips_spaces_and_lowercases_text():
    ...


def test_withdraw_raises_error_when_balance_is_insufficient():
    ...
```

不推荐：

```python
def test_1():
    ...
```

## 一个测试只验证一个核心行为

不推荐：

```python
def test_user():
    # 测创建
    # 测修改
    # 测删除
    # 测异常
```

更推荐拆分：

```python
def test_create_user():
    ...


def test_update_user_name():
    ...


def test_create_user_raises_error_when_name_is_empty():
    ...
```

## AAA 结构

Arrange：准备。

Act：执行。

Assert：断言。

```python
def test_cart_total_price():
    cart = ShoppingCart()
    cart.add_item("apple", 3, 2)

    total = cart.total_price()

    assert total == 6
```

## 测试数据

测试数据要小而明确。

不要用巨大真实文件测试简单函数。真实文件可以放到集成测试或专门样例中。

## conftest.py

共享 fixture 放 `conftest.py`。

不要把业务函数放进 `conftest.py`。

## 单元测试和集成测试

单元测试：

- 测单个函数或类。
- 快。
- 依赖少。

集成测试：

- 测多个模块协作。
- 可能涉及文件、数据库、网络替身。
- 数量少但覆盖关键链路。

## 常见错误

### 测试依赖顺序

测试 A 先创建数据，测试 B 再使用，这会让测试脆弱。

### 测试太大

失败时不知道哪个行为坏了。

### 命名看不出意图

失败报告无法帮助定位。

## 练习

1. 为 `text_tools` 建 `tests/` 目录。
2. 给每个模块创建对应测试文件。
3. 把一个大测试拆成 3 个小测试。
4. 按 AAA 结构重写一个测试。
5. 给 10 个测试函数重命名。
6. 区分 5 个测试属于单元还是集成。

## 验收标准

- 能组织 `tests/` 目录。
- 能写表达场景的测试名。
- 能使用 AAA 结构。
- 能区分单元测试和集成测试。
- 能避免测试顺序依赖。

