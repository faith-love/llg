# 练习清单和验收复盘

第 09 阶段练习要覆盖数据库、SQL、Python 数据库访问、pandas、ETL、质量校验和工程化。完成后你应该能交付一个小型数据处理项目。

## A. 数据库基础

1. 设计 users 表。
2. 设计 orders 表。
3. 设计 order_items 表。
4. 给 users.email 增加唯一约束。
5. 给 orders.status 设计枚举。
6. 写三张表的数据字典。
7. 画出表关系。
8. 说明主键和外键。
9. 说明一个范式设计。
10. 说明一个反范式字段。

## B. SQL

11. 查询所有用户。
12. 查询已支付订单。
13. 按用户统计订单数。
14. 按状态统计订单金额。
15. JOIN 用户和订单。
16. LEFT JOIN 找无订单用户。
17. HAVING 筛选订单数超过 3 的用户。
18. 写一个 CTE 查询。
19. UPDATE 前先 SELECT。
20. DELETE 前先 SELECT。

## C. SQLite 和 DB-API

21. 创建 SQLite 数据库。
22. 创建 users 表。
23. 插入用户。
24. 参数化查询用户。
25. 更新用户。
26. 删除用户。
27. 使用事务提交。
28. 使用事务回滚。
29. 使用 `sqlite3.Row`。
30. 使用内存数据库测试。

## D. SQLAlchemy

31. 创建 engine。
32. 使用 Core 定义表。
33. 使用 Core 插入数据。
34. 使用 Core 查询数据。
35. 定义 ORM User 模型。
36. 使用 Session 新增用户。
37. 使用 Session 查询用户。
38. 使用 Session 更新用户。
39. 捕获异常并 rollback。
40. 观察 ORM 生成的 SQL。

## E. pandas 基础

41. 创建 Series。
42. 创建 DataFrame。
43. 选择列。
44. 过滤行。
45. 新增列。
46. 查看 dtypes。
47. 读取 CSV。
48. 读取 Excel。
49. 读取 JSON。
50. 导出 CSV。

## F. 数据清洗

51. 标准化字段名。
52. 清理字符串空格。
53. 统一大小写。
54. 统计缺失值。
55. 转换金额类型。
56. 转换日期类型。
57. 找出转换失败记录。
58. 处理重复订单号。
59. 输出异常报告。
60. 写清洗规则说明。

## G. 数据汇总和合并

61. groupby 统计订单数。
62. groupby 汇总金额。
63. agg 生成多个指标。
64. pivot_table 生成报表。
65. rolling 计算滚动指标。
66. rank 做排名。
67. concat 合并多文件。
68. merge 订单和用户。
69. merge 使用 validate。
70. merge 使用 indicator。

## H. 数据质量和 ETL

71. 校验必填字段。
72. 校验唯一性。
73. 校验金额范围。
74. 校验状态枚举。
75. 校验外键关系。
76. 生成质量报告。
77. 拆分 extract。
78. 拆分 transform。
79. 拆分 load。
80. 生成 batch_id。

## I. 增量和幂等

81. 设计 watermark 表。
82. 使用 updated_at 增量读取。
83. 成功后更新水位。
84. 失败不更新水位。
85. 设计唯一键。
86. 实现 upsert。
87. 记录 processed_files。
88. 计算文件 checksum。
89. 设计迟到数据回看。
90. 重跑验证不重复。

## J. 综合项目

91. SQLite CRUD 项目。
92. SQLAlchemy ORM 项目。
93. pandas 清洗项目。
94. CSV 到数据库 ETL。
95. Excel 到清洗报告项目。
96. 增量同步项目。
97. 带质量阈值的数据任务。
98. 带日志和血缘报告的数据任务。
99. 给数据项目补充测试。
100. 写完整阶段复盘。

## 综合项目验收

至少完成 4 个项目：

- SQLite CRUD 小项目。
- SQLAlchemy ORM 小项目。
- pandas 数据清洗项目。
- ETL 入库项目。

每个项目必须包含：

- README。
- 运行命令。
- 输入样例。
- 输出样例。
- 错误样例。
- 验证方式。

## 阶段验收

通过标准：

- 能设计基本关系表。
- 能写安全 SQL。
- 能用 Python 完成 CRUD。
- 能解释事务、索引和连接池。
- 能用 SQLAlchemy 完成基础 ORM。
- 能用 pandas 清洗、汇总、合并数据。
- 能输出数据质量报告。
- 能设计 ETL 流程。
- 能实现增量同步和幂等写入。
- 能写日志、血缘和复盘。

## 复盘问题

1. 哪个表设计最容易变复杂？
2. 哪条 SQL 最容易写错？
3. 哪个字段类型最容易出问题？
4. 哪个清洗规则可能误伤数据？
5. 哪个 merge 可能造成行数膨胀？
6. 哪些数据必须进入异常报告？
7. 你的 ETL 能否重复运行？
8. 失败后如何恢复？
9. 如何证明数据没有重复写入？
10. 下一阶段工程化部署时需要补哪些检查？
