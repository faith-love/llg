# 字典基础

字典用于保存键值对，是 Python 中最重要的数据结构之一。它适合表达“通过某个键快速找到某个值”的关系。

## 创建字典

```python
user = {
    "name": "Alice",
    "age": 18,
    "city": "Shanghai",
}
```

空字典：

```python
data = {}
```

注意：`{}` 是空字典，不是空集合。空集合用 `set()`。

## 键和值

字典由键和值组成：

```python
{
    "name": "Alice"
}
```

- `"name"` 是键。
- `"Alice"` 是值。

键必须可哈希，常见键类型：

- 字符串。
- 数字。
- 元组。

列表、字典、集合不能作为字典键。

## 访问值

```python
print(user["name"])
```

如果键不存在，会报 `KeyError`。

## 使用 `get`

```python
print(user.get("name"))
print(user.get("email"))
print(user.get("email", "未填写"))
```

`get` 找不到键时默认返回 `None`，也可以指定默认值。

## 添加和修改

```python
user["email"] = "alice@example.com"
user["age"] = 19
```

如果键不存在，就是添加。

如果键已存在，就是修改。

## 删除键值对

### `pop`

```python
age = user.pop("age")
```

可以提供默认值，避免键不存在报错：

```python
email = user.pop("email", None)
```

### `del`

```python
del user["city"]
```

键不存在会报 `KeyError`。

### `clear`

```python
user.clear()
```

## 判断键是否存在

```python
if "name" in user:
    print(user["name"])
```

`in` 判断的是键，不是值。

## 字典长度

```python
print(len(user))
```

表示键值对数量。

## 字典嵌套

```python
user = {
    "name": "Alice",
    "address": {
        "city": "Shanghai",
        "district": "Pudong",
    },
}

print(user["address"]["city"])
```

嵌套字典在 JSON 和配置处理中非常常见。

## 常见错误

### 键不存在

```python
print(user["email"])
```

如果不确定键是否存在，用 `get` 或先判断。

### 把空集合写成 `{}`

```python
empty = {}
print(type(empty))
```

这是字典。

### 用列表作为键

```python
data = {[1, 2]: "value"}
```

会报 `TypeError`。

## 练习

1. 创建一个用户字典。
2. 读取用户名和年龄。
3. 添加邮箱字段。
4. 修改年龄。
5. 删除城市字段。
6. 使用 `get` 读取不存在的字段。
7. 判断某个键是否存在。

## 验收标准

- 能创建、访问、修改、删除字典。
- 能区分 `[]` 访问和 `get`。
- 能解释字典键的基本要求。
- 能避免空字典和空集合混淆。

