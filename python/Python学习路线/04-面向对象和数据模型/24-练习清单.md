# 练习清单

本阶段至少完成 70 个练习，覆盖类、实例、属性、方法、封装、组合、继承、多态、dataclass、Enum、特殊方法和领域建模。

## A. 类和实例

1. 定义空类 `Book` 并创建实例。
2. 创建两个实例，比较 `is`。
3. 给实例添加 `title` 属性。
4. 定义 `show_title` 方法。
5. 使用 `type`、`id`、`isinstance` 观察对象。
6. 使用 `__dict__` 查看实例属性。
7. 复现不同实例属性不一致问题。
8. 复现忘记 `self` 的错误。

## B. 初始化和属性

9. 写 `User(name, age)`。
10. 在 `__init__` 中校验年龄非负。
11. 写 `Book(title, author, available=True)`。
12. 复现 `__init__` 返回值错误。
13. 复现可变默认参数问题。
14. 用 `None` 修复默认成员列表。
15. 复制传入列表，避免外部修改影响内部。

## C. 方法类型和属性边界

16. 写实例方法 `greet`。
17. 写类方法 `from_dict`。
18. 写静态方法 `is_valid_name`。
19. 定义类属性 `species`。
20. 观察实例属性遮蔽类属性。
21. 复现可变类属性共享。
22. 写只读 `full_name` property。
23. 写带校验的 `price` property。
24. 复现 property 递归错误。

## D. 组合和继承

25. 设计 `Address` 和 `User` 组合。
26. 设计 `Order` 和 `OrderItem` 组合。
27. 设计 `Library` 和 `Book` 组合。
28. 定义 `Animal` 和 `Dog` 继承。
29. 子类重写父类方法。
30. 使用 `super().__init__`。
31. 复现忘记调用 `super()` 的问题。
32. 打印多继承 MRO。
33. 把错误继承关系改成组合。

## E. 多态和抽象

34. 写 `Dog` 和 `Cat`，都实现 `speak`。
35. 写 `make_sound(animal)`。
36. 写两个 Writer，都实现 `write`。
37. 写两个折扣策略，都实现 `apply`。
38. 把 `if type(...)` 分支改成多态。
39. 定义 `Exporter` 抽象基类。
40. 实现 `JsonExporter`。
41. 尝试实例化抽象类。
42. 判断 5 个场景用鸭子类型还是抽象基类。

## F. dataclass 和 Enum

43. 定义 `User` 数据类。
44. 定义带默认值的 `Product`。
45. 使用 `field(default_factory=list)`。
46. 定义不可变 `Point`。
47. 使用 `__post_init__` 校验价格。
48. 使用 `repr=False` 隐藏密码字段。
49. 定义 `OrderStatus` 枚举。
50. 输出枚举 `name` 和 `value`。
51. 从字符串值创建枚举。
52. 使用枚举写订单状态判断。

## G. 特殊方法

53. 给 `Book` 实现 `__repr__`。
54. 给 `Book` 实现 `__str__`。
55. 按 ISBN 实现 `__eq__`。
56. 给 `Score` 实现 `__lt__`。
57. 给 `Bookshelf` 实现 `__len__`。
58. 给 `Bookshelf` 实现 `__contains__`。
59. 给 `Bookshelf` 实现 `__iter__`。
60. 给 `Bookshelf` 实现 `__getitem__`。
61. 写 `Timer` 上下文管理器。
62. 观察 `__exit__` 返回 `True` 的效果。

## H. 领域建模综合

63. 图书馆模型：图书、会员、借阅记录。
64. 购物车模型：商品、购物车项、购物车。
65. 订单模型：订单、订单项、订单状态枚举。
66. 银行账户模型：存款、取款、余额不足异常。
67. 报告导出模型：抽象导出器、JSON 导出器、文本导出器。
68. 书架容器模型：支持 `len`、遍历和成员判断。
69. 用 dataclass 重写一个数据对象。
70. 用组合重构一个继承滥用示例。
71. 为一个模型写领域异常。
72. 为一个对象实现清晰 `repr`。

## 每个练习的要求

- 类名表达领域概念。
- 方法名表达行为。
- 属性初始化完整。
- 不滥用继承。
- 对象规则由对象自己维护。
- 错误信息清晰。
- 特殊方法语义符合直觉。

