# 12-多实例任务和分布式锁

## 多实例下为什么任务会重复跑

如果应用部署成多个实例，而每个实例都启用了同一个定时任务，就会出现重复执行。

这类问题常见于：

- 报表统计。
- 对账。
- 过期清理。

## 分布式锁的意义

它解决的是：

- 同一时刻只让一个实例执行关键任务。

但锁不是万能药，还要考虑：

- 锁超时。
- 锁续期。
- 任务执行失败。

## 本节通过标准

- 能意识到多实例任务重复执行风险。
- 能理解分布式锁的基本作用和限制。
- 能为关键任务补最基本的执行保护。
