# 13-环境配置、密钥和部署参数

## 环境参数为什么要独立管理

部署环境最容易变化的是：

- 数据库地址
- 用户名密码
- 第三方密钥
- 日志级别

这些不应该和业务代码混在一起。

## 密钥管理

敏感信息至少要避免：

- 明文进仓库。
- 同一份测试配置直接复制到生产。

## 运行时参数

要能明确：

- 哪些参数必须传。
- 哪些有默认值。
- 哪些会覆盖 Profile 配置。

## 本节通过标准

- 能建立环境参数和敏感信息的基本分离意识。
- 能说明运行时参数和静态配置的关系。
- 能避免把密钥和账号写死在仓库中。
