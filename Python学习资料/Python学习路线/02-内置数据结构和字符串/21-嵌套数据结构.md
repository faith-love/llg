# 嵌套数据结构

真实数据很少是简单列表或简单字典。接口返回、JSON 文件、配置、表格转换结果通常都是列表套字典、字典套列表、字典套字典。

## 列表套字典

最常见的数据形态：

```Python学习资料
用户s = [
    {"name": "Alice", "age": 18, "city": "Shanghai"},
    {"name": "Bob", "age": 20, "city": "Beijing"},
]
```

读取：

```Python学习资料
print(用户s[0]["name"])
```

遍历：

```Python学习资料
for 用户 in 用户s:
    print(用户["name"], 用户["age"])
```

## 字典套列表

```Python学习资料
groups = {
    "Shanghai": ["Alice", "Cindy"],
    "Beijing": ["Bob"],
}
```

读取：

```Python学习资料
print(groups["Shanghai"])
```

遍历：

```Python学习资料
for city, names in groups.items():
    print(city, names)
```

## 字典套字典

```Python学习资料
用户s_by_id = {
    1: {"name": "Alice", "age": 18},
    2: {"name": "Bob", "age": 20},
}
```

按 ID 查找：

```Python学习资料
用户 = 用户s_by_id.get(1)
```

## 安全读取

直接读取：

```Python学习资料
city = 用户["address"]["city"]
```

如果 `address` 不存在，会报错。

更安全：

```Python学习资料
address = 用户.get("address", {})
city = address.get("city", "未知")
```

不要为了避免报错无脑写很长的链式 `get`，复杂时拆成中间变量更清楚。

## 修改嵌套数据

```Python学习资料
用户s[0]["age"] = 19
groups["Shanghai"].append("David")
用户s_by_id[1]["city"] = "Shanghai"
```

修改嵌套结构时，要确认自己是在修改原数据，还是应该创建新数据。

## 从列表套字典提取字段

```Python学习资料
names = []

for 用户 in 用户s:
    names.append(用户["name"])
```

推导式写法：

```Python学习资料
names = [用户["name"] for 用户 in 用户s]
```

## 过滤嵌套数据

```Python学习资料
adults = []

for 用户 in 用户s:
    if 用户["age"] >= 18:
        adults.append(用户)
```

## 转换结构

列表转 ID 字典：

```Python学习资料
用户s_by_id = {}

for 用户 in 用户s:
    用户s_by_id[用户["id"]] = 用户
```

按城市分组：

```Python学习资料
groups = {}

for 用户 in 用户s:
    city = 用户["city"]
    groups.setdefault(city, []).append(用户)
```

## 常见错误

### 层级判断不清

先问自己：

- 当前变量是列表还是字典。
- 这一层应该用索引还是键。
- 取出来的下一层是什么类型。

### 链式访问过长

```Python学习资料
value = 数据["a"]["b"]["c"]["d"]
```

不利于调试。可以拆开：

```Python学习资料
a = 数据["a"]
b = a["b"]
c = b["c"]
value = c["d"]
```

### 修改共享对象

嵌套结构浅拷贝后，内部对象可能共享。

## 练习

1. 创建用户列表，每个用户是字典。
2. 提取所有用户名。
3. 筛选成年用户。
4. 按城市分组用户。
5. 把用户列表转换成按 ID 查找的字典。
6. 安全读取用户地址里的城市。
7. 修改某个用户的年龄。
8. 复现浅拷贝修改嵌套数据的问题。

## 验收标准

- 能读写列表套字典。
- 能读写字典套列表。
- 能把列表转换成查找字典。
- 能按字段过滤和分组嵌套数据。
- 能拆解复杂链式访问。

