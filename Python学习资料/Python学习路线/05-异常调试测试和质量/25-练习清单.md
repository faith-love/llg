# 练习清单

本阶段至少完成 70 个练习，覆盖异常、调试、日志、unit测试、py测试、mock、覆盖率、格式化和静态检查。

## A. 异常基础

1. 制造并解释 `ValueError`。
2. 制造并解释 `KeyError`。
3. 制造并解释 `AttributeError`。
4. 写三层函数调用，让异常传播。
5. 捕获 `ValueError` 并输出清晰提示。
6. 分别捕获 `KeyError` 和 `ValueError`。
7. 把捕获范围过大的 `try` 拆小。
8. 删除裸 `except`。
9. 使用 `else` 处理成功逻辑。
10. 使用 `finally` 做收尾。

## B. raise 和自定义异常

11. 写 `divide`，除数为 0 时 raise。
12. 写 `validate_age`，非法年龄时 raise。
13. 重新抛出当前异常。
14. 使用 `raise from` 包装异常。
15. 定义 `AppError`。
16. 定义 `ValidationError`。
17. 定义 `InsufficientBalanceError`。
18. 写入口层统一捕获 `AppError`。
19. 把底层 `print + exit` 改成 raise。
20. 给自定义异常添加字段。

## C. traceback 和调试

21. 阅读一个三层 traceback。
22. 标出异常类型、文件、行号。
23. 抽取一个最小复现。
24. 使用 `repr` 观察隐藏空格。
25. 使用 `traceback.print_exc()`。
26. 使用 `breakpoint()`。
27. 用 p未译66984 的 `p` 查看变量。
28. 用 p未译66984 的 `n` 单步执行。
29. 调试一个分支判断错误。
30. 调试一个循环边界错误。

## D. 日志ging

31. 配置 `日志ging.basicConfig`。
32. 输出 DEBUG、INFO、WA未译25173NING、E未译25173未译25173O未译25173。
33. 创建模块级 日志ger。
34. 配置日志格式显示时间和模块名。
35. 使用 `日志ger.异常`。
36. 把 f-string 日志改成占位符。
37. 输出日志到文件。
38. 检查日志是否包含敏感信息。

## E. unit测试 和 py测试

39. 用 unit测试 测试 `add`。
40. 用 unit测试 测试异常。
41. 安装并运行 py测试。
42. 写 `测试_add`。
43. 故意让 py测试 失败，阅读输出。
44. 用 `-v` 运行 py测试。
45. 运行指定测试函数。
46. 给购物车写 fixture。
47. 把共享 fixture 移到 `conf测试.py`。
48. 使用参数化测试分数等级。
49. 使用参数化测试年龄边界。
50. 使用 `py测试.raises` 测试异常。
51. 测试异常后对象状态不变。

## F. mock 和文件测试

52. 使用 `Mock` 记录函数调用。
53. 使用 `assert_called_once_with`。
54. 使用 monkeypatch 设置环境变量。
55. 使用 monkeypatch 替换函数。
56. 使用 `tmp_path` 测试文件写入。
57. 判断 5 个依赖是否应该 mock。
58. 把过度 mock 的测试改成结果导向测试。

## G. 测试组织和覆盖率

59. 建立 `测试s/` 目录。
60. 给每个模块创建对应测试文件。
61. 把一个大测试拆成 3 个小测试。
62. 按 AAA 结构重写测试。
63. 安装 py测试-cov。
64. 生成覆盖率报告。
65. 为异常分支补测试。
66. 为一个 bug 写回归测试。

## H. 质量工具和文档

67. 安装并运行 Black 或 未译25173uff 未译50816at。
68. 安装并运行 isort 或 未译25173uff check。
69. 修复未使用导入。
70. 给 5 个核心函数添加类型标注。
71. 安装并运行 mypy。
72. 找一个复杂函数并拆分。
73. 给 5 个函数写 文档tring。
74. 删除 5 条无意义注释。
75. 改写 3 条模糊 待办。

## 综合练习

76. 给图书馆模型补自定义异常。
77. 给图书馆模型补 py测试 测试。
78. 给购物车模型补异常测试。
79. 给文本工具包补日志。
80. 给文本工具包运行 未译25173uff 和 py测试。

## 每个练习的要求

- 错误信息清晰。
- 测试能独立运行。
- 测试名表达场景。
- 异常分支有覆盖。
- 日志不泄露敏感信息。
- 工具检查结果有记录。

