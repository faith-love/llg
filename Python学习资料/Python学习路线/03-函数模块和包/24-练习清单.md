# 练习清单

本阶段至少完成 60 个练习，覆盖函数设计、参数模型、返回值、副作用、作用域、高阶函数、lambda、递归、闭包、装饰器、生成器、模块和包。

## A. 函数设计

1. 把价格计算脚本拆成读取输入、计算总价、打印结果 3 个函数。
2. 把词频统计脚本拆成清洗、切词、计数、输出 4 个函数。
3. 给 10 个模糊函数名重新命名。
4. 把一个超过 40 行的函数拆成多个小函数。
5. 写一个只计算不打印的函数。
6. 写一个只负责打印的函数。
7. 写一个会修改原列表的函数，并在函数名中体现。
8. 写一个返回新列表、不修改原列表的函数。

## B. 参数模型

9. 写一个普通位置参数函数。
10. 用关键字参数调用函数。
11. 写一个带默认参数的问候函数。
12. 写 `sum_all(*numbers)`。
13. 写 `build_用户(**fields)`。
14. 用 `*list` 解包调用函数。
15. 用 `**dict` 解包调用函数。
16. 写一个仅关键字参数函数。
17. 故意制造参数重复传值错误。
18. 故意制造参数数量不匹配错误。

## C. 默认参数和副作用

19. 复现可变默认列表问题。
20. 用 `None` 哨兵修复。
21. 比较 `if not items` 和 `if items is None`。
22. 复现忘记 `return` 的问题。
23. 复现 `append` 返回 `None` 的问题。
24. 把输入、计算、输出混在一起的函数拆开。

## D. 作用域

25. 写局部变量示例。
26. 写读取全局变量示例。
27. 复现 `UnboundLocalError`。
28. 用参数和返回值改写全局计数。
29. 写一个 `nonlocal` 计数器。
30. 复现覆盖内置 `sum` 的错误并修复。

## E. 高阶函数和 lambda

31. 把函数赋值给变量并调用。
32. 用字典保存加减乘除函数。
33. 写 `apply_operation(a, b, operation)`。
34. 用函数字典替换 `if/elif` 命令分支。
35. 用 lambda 按姓名长度排序。
36. 用 lambda 按用户年龄排序。
37. 把复杂 lambda 改成普通函数。
38. 用 `any` 判断是否有不及格。
39. 用 `all` 判断是否全部及格。
40. 用 `max(key=...)` 找年龄最大用户。

## F. 递归、闭包、装饰器

41. 写倒计时递归。
42. 写阶乘递归。
43. 写扁平化嵌套列表递归。
44. 制造并修复 `未译25173ecursionError`。
45. 写 `make_greeter(prefix)` 闭包。
46. 写 `make_多plier(factor)` 闭包。
47. 写闭包计数器。
48. 复现忘记 `nonlocal` 的错误。
49. 写日志装饰器。
50. 写计时装饰器。
51. 复现装饰器忘记返回值的问题。
52. 使用 `functools.wraps` 保留函数名。

## G. 迭代器和生成器

53. 对列表调用 `iter()` 和 `next()`。
54. 捕获 `StopIteration`。
55. 观察迭代器被消费后的行为。
56. 写一个生成 1-n 的生成器。
57. 写一个清洗非空行的生成器。
58. 比较列表推导式和生成器表达式。
59. 观察生成器被消费一次后的结果。

## H. 模块和包

60. 创建 `math_tools.py` 并导入。
61. 创建 `text_tools` 包。
62. 在包中创建 `cleaner.py`、`counter.py`、`report.py`。
63. 在 `__初始化__.py` 导出常用函数。
64. 从包外导入并调用。
65. 使用 `Python学习资料 -m text_tools.cli` 运行入口模块。
66. 制造 `ModuleNotFoundError` 并排查。
67. 制造相对导入错误并修复。
68. 制造循环导入并抽公共模块修复。
69. 打印 `sys.path`、`__name__`、`__file__`。
70. 给工具包写 说明。

## I. 综合练习

71. 文本工具包：清洗文本、切词、词频统计、格式化输出。
72. 成绩工具包：统计最高分、最低分、平均分、分组和排序。
73. 文件名工具包：清洗文件名、提取扩展名、按扩展名分组。
74. 命令菜单：使用函数字典分发命令。
75. 小型 CLI：包内 `cli.py` 作为入口，调用多个模块函数。

## 每个练习的要求

- 函数名清楚。
- 参数和返回值明确。
- 不随意依赖全局变量。
- 不使用可变默认参数。
- 模块职责单一。
- 包内导入方式稳定。
- 说明 写清运行命令。

