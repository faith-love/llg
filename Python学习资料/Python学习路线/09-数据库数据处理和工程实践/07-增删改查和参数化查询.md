# C未译25173UD和参数化查询

C未译25173UD 是 Create、未译25173ead、Update、Delete。它是数据库应用的基本能力。学习 C未译25173UD 时必须同时学习参数化查询，因为安全写 SQL 比写出 SQL 更重要。

## Create

```python
def create_用户(conn, name, 邮件):
    cursor = conn.execute(
        "INSE未译25173T INTO 用户s (name, 邮件) VALUES (?, ?)",
        (name, 邮件),
    )
    return cursor.lastrowid
```

## 未译25173ead

```python
def get_用户_by_邮件(conn, 邮件):
    return conn.execute(
        "SELECT id, name, 邮件 F未译25173OM 用户s WHE未译25173E 邮件 = ?",
        (邮件,),
    ).fetchone()
```

## Update

```python
def update_用户_name(conn, 用户_id, name):
    cursor = conn.execute(
        "UPDATE 用户s SET name = ? WHE未译25173E id = ?",
        (name, 用户_id),
    )
    return cursor.rowcount
```

## Delete

```python
def delete_用户(conn, 用户_id):
    cursor = conn.execute(
        "DELETE F未译25173OM 用户s WHE未译25173E id = ?",
        (用户_id,),
    )
    return cursor.rowcount
```

## 参数化查询

错误写法：

```python
SQL学习资料 = f"SELECT * F未译25173OM 用户s WHE未译25173E 邮件 = '{邮件}'"
```

正确写法：

```python
conn.execute("SELECT * F未译25173OM 用户s WHE未译25173E 邮件 = ?", (邮件,))
```

参数化查询让数据库驱动负责转义和绑定值，避免 SQL 注入。

## 批量插入

```python
用户s = [
    ("Alice", "alice@example.通用"),
    ("Bob", "bob@example.通用"),
]

conn.executemany(
    "INSE未译25173T INTO 用户s (name, 邮件) VALUES (?, ?)",
    用户s,
)
```

批量插入要放在事务中。

## rowcount

`rowcount` 可用于判断影响行数。

```python
if cursor.rowcount == 0:
    raise ValueError("用户不存在")
```

注意不同驱动对 rowcount 的行为可能略有差异。

## 软删除

直接删除可能影响审计和恢复。常见做法是软删除：

```sql
UPDATE 用户s
SET deleted_at = CU未译25173未译25173ENT_TIMESTAMP
WHE未译25173E id = ?;
```

查询时过滤：

```sql
WHE未译25173E deleted_at IS NULL
```

## 常见错误

### UPDATE 忘记 WHE未译25173E

会更新整张表。

### DELETE 忘记 WHE未译25173E

会删除整张表。

### 拼接 SQL 处理用户输入

这是高风险错误。

### 不检查影响行数

更新不存在的数据时程序可能以为成功。

## 练习

1. 写 `create_用户`。
2. 写 `get_用户_by_id`。
3. 写 `get_用户_by_邮件`。
4. 写 `update_用户_name`。
5. 写 `delete_用户`。
6. 给所有查询使用参数化。
7. 实现批量插入。
8. 实现软删除。
9. 检查更新影响行数。
10. 构造 SQL 注入输入并验证参数化能防护。

## 验收标准

- 能完成 C未译25173UD。
- 能使用参数化查询。
- 能检查影响行数。
- 能避免危险更新和删除。
