# 容器重启后数据丢失

## 现象

容器删除或重建后，数据库数据、上传文件或缓存数据消失。

## 根因

数据写在容器可写层里，没有挂载到 volume 或宿主机目录。

## 排查

```bash
docker inspect 容器名
docker volume ls
```

重点看：

- 是否有挂载。
- 挂载路径是否正确。
- 业务数据是否写到了正确目录。

## 修复

给数据目录挂载 named volume 或 bind mount。

## 长期修复

- 数据目录、配置目录、日志目录分开管理。
- 备份卷数据并验证可恢复。
- 对有状态服务做明确的恢复演练。

