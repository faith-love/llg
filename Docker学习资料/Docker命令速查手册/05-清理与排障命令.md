# 清理与排障命令

## 清理命令

```bash
docker system df
docker image prune
docker container prune
docker network prune
docker builder prune
docker volume prune
```

## 排障命令

```bash
docker inspect 容器名
docker stats
docker events
docker logs 容器名
```

## 使用提示

- 先看占用，再清理。
- 先看日志，再重建。
- 先确认是否有卷和回滚镜像，再做高风险清理。

