# 10-未译61653

## 服务调用不是本地方法

Feign 让调用写起来像本地接口，但本质仍然是网络调用。

所以你必须设计：

- 超时
- 重试
- 熔断
- 降级

## 重试要小心副作用

如果一个操作不是天然幂等，重试可能带来重复执行。

所以超时和重试不能脱离业务语义单独配置。

## 熔断的价值

熔断不是为了“好看”，而是为了：

- 防止故障扩散
- 保护上游线程和资源

## 本节通过标准

- 能解释 Feign 调用为什么仍然要认真设计失败路径。
- 能区分超时、重试、熔断、降级的作用。
- 能意识到幂等和重试的关系。


