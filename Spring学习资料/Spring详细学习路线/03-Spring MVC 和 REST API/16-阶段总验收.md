# 16-阶段总验收

## 这一阶段结束前，至少确认这些点

- 能画出请求处理主线。
- 能按资源设计 CRUD 接口。
- 能区分 `@PathVariable`、`@RequestParam`、`@RequestBody`。
- 能用 DTO 隔离输入输出边界。
- 能处理参数校验、业务异常和系统异常。
- 能设计分页、排序和筛选参数。
- 能注意时间、枚举和 Long 精度问题。
- 能为主要接口补文档和示例。

## 通过标准

能独立做出一个结构清楚、错误可控的图书 REST API，就可以进入更重的 Boot、事务和安全工程主题。


