# 16-阶段总验收

## 这一阶段结束前，至少确认这些点

- 能解释 AOP 适合和不适合的场景。
- 能区分前置、后置和环绕通知。
- 能理解切点匹配策略和代理限制。
- 能区分同步、异步和事务事件。
- 能处理嵌套校验、统一错误和结构化配置读取。
- 能区分 `@Value`、`Environment`、`Resource`、`@ConfigurationProperties` 的边界。

## 通过标准

能把横切逻辑、事件解耦、输入校验和配置读取边界讲清楚，这一阶段就算真正吃透了。


