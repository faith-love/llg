# 06-JUnit边界和假对象使用

## JUnit要测什么

JUnit更适合纯业务逻辑：

- 条件分支。
- 金额计算。
- 状态流转。
- 参数组合。

## 不要为了“像真环境”把单测写重

JUnit的目标是快、准、定位清楚。

如果每个单测都要启动 Spring Docker，它很快就不再是轻量JUnit。

## 假对象的价值

测试替身可以帮助你：

- 隔离外部依赖。
- 聚焦当前逻辑。
- 控制边界条件。

## 本节通过标准

- 能区分JUnit和集成测试边界。
- 能用假对象隔离外部依赖。
- 能保持JUnit轻量和可定位。


