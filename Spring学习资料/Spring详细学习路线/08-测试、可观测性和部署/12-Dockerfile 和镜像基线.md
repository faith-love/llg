# 12-Dockerfile 和镜像基线

## Dockerfile 不是把 jar 复制进去就结束

最小可用之外，还要考虑：

- 基础镜像选择。
- 时区和编码。
- 启动命令。
- 暴露端口。

## 为什么镜像基线重要

统一镜像基线可以减少：

- 环境差异。
- 启动行为漂移。
- 生产排障成本。

## 本节通过标准

- 能写一个最小可用 Dockerfile。
- 能知道镜像基线为什么重要。
- 能避免只在本机 jar 运行层面思考部署。


