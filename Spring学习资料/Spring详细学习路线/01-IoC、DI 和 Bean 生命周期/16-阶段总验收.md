# 16-阶段总验收

## 这一阶段结束前，至少确认这些点

- 能解释 IoC 和 DI 的关系。
- 能区分 `@Component` 和 `@Bean` 的使用场景。
- 能优先使用构造器注入。
- 能理解 `@Primary`、`@Qualifier` 和多实现注入。
- 能理解生命周期、Aware 和 BeanPostProcessor 的位置。
- 能区分 singleton 和线程安全。
- 能从设计角度看循环依赖。

## 通过标准

能把 Bean 管理从“会加注解”推进到“能解释Docker边界、注入策略和生命周期”，这一阶段就算真正稳了。


